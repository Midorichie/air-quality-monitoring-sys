{
  "name": "air-quality-monitoring-tests",
  "version": "1.0.0",
  "description": "Tests for Air Quality Monitoring Smart Contract",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@stacks/transactions": "^6.0.0"
  },
  "devDependencies": {
    "@hirosystems/clarinet-sdk": "^2.12.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.6.0"
  }
}
